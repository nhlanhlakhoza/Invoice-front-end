import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'invoice.app',
  appName: 'com.example.app',
  webDir: 'dist/invoice-app-f'
};

export default config;
